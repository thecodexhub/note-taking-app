/// {@template authentication_repository}
/// Dart package that manages the authentication domain
/// {@endtemplate}
class AuthenticationRepository {
  /// {@macro authentication_repository}
  const AuthenticationRepository();
}
